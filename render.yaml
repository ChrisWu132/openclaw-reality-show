services:
  - type: web
    name: openclaw-reality-show
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
